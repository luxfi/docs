---
title: Installation
description: Set up the Lux Bridge
icon: Download
---

## Bridge UI

```bash
git clone https://github.com/luxfi/bridge
cd bridge/ui && npm install && npm run dev
```

## Bridge API

```bash
cd bridge/server && npm install && npm start
```
